apiVersion: v1
kind: Service
metadata:
  name: add-task
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  selector:
    app: add-task
  ports:
    - port: 80
      targetPort: 8000
