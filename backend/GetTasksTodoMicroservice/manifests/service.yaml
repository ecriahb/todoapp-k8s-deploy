apiVersion: v1
kind: Service
metadata:
  name: agentic-get-tasks
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  selector:
    app: agentic-get-tasks
  ports:
    - port: 80
      targetPort: 8000
