apiVersion: v1
kind: Service
metadata:
  name: agentic-add-task
  annotations:
    # Internal LoadBalancer in Azure
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  selector:
    app: agentic-add-task
  ports:
    - port: 80
      targetPort: 8000
