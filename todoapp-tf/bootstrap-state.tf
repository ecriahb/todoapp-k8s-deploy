# Remote-state bootstrap is maintained separately under todoapp-tf/bootstrap/.
# This file intentionally contains no active resources so the main Terraform
# module does not attempt to create the backend after initialization.
